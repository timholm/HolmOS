---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-web-nautilus
  namespace: holm
  labels:
    app: file-web-nautilus
    holmos.io/component: file-nautilus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-web-nautilus
  template:
    metadata:
      labels:
        app: file-web-nautilus
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: file-web-nautilus
        image: 192.168.8.197:30500/file-web-nautilus:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: file-web-nautilus
  namespace: holm
  labels:
    app: file-web-nautilus
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30088
    protocol: TCP
    name: http
  selector:
    app: file-web-nautilus
