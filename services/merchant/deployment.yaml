---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant
  namespace: holm
  labels:
    app: merchant
    holmos.io/component: merchant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: merchant
  template:
    metadata:
      labels:
        app: merchant
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: merchant
        image: 192.168.8.197:30500/merchant:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: merchant
  namespace: holm
  labels:
    app: merchant
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30016
    protocol: TCP
    name: http
  selector:
    app: merchant
